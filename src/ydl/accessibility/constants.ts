/**
 * YouTrader accessibility constants (guidance, not global enforcement).
 */

/** Apple HIG minimum interactive target (pt). */
export const YDL_MIN_TOUCH_TARGET = 44;

/**
 * Recommended max fontScale for dense terminal chrome only.
 * Do NOT apply globally. Cap only with an explicit component-level reason.
 */
export const YDL_FONT_SCALE_DENSE_UI_MAX = 1.35;
