/** Default static frame used when Reduce Motion disables continuous playback. */
export const YDL_LOTTIE_REDUCE_MOTION_PROGRESS = 0;

/** Soft upper guidance for local JSON assets (bytes). Enforce in review. */
export const YDL_LOTTIE_MAX_JSON_BYTES = 80_000;
